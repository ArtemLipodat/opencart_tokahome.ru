{#<div class="list-group">#}
{#  {% for category in categories %}#}
{#  {% if category.category_id == category_id %} #}
{#  <a href="{{ category.href }}" class="list-group-item active">{{ category.name }}</a> #}
{#  {% if category.children %}#}
{#  {% for child in category.children %}#}
{#  {% if child.category_id == child_id %}#}
{#  <a href="{{ child.href }}" class="list-group-item active">&nbsp;&nbsp;&nbsp;- {{ child.name }}</a> #}
{#  {% else %} #}
{#  <a href="{{ child.href }}" class="list-group-item">&nbsp;&nbsp;&nbsp;- {{ child.name }}</a>#}
{#  {% endif %}#}
{#  {% endfor %}#}
{#  {% endif %}#}
{#  {% else %} <a href="{{ category.href }}" class="list-group-item">{{ category.name }}</a>#}
{#  {% endif %}#}
{#  {% endfor %}#}
{#</div>#}

<div class="catalog">
  {% for category in categories %}
  <div class="catalog-item">
    <a href="{{ category.href }}"><img src="{{ category.img }}" alt=""></a>
    <div class="catalog-item-text">
      <a href="{{ category.href }}">{{ category.name }}</a>
      <a href="{{ category.href }}">{{ category.desc }}</a>
    </div>
{#    <a class="catalog-item-button" href="{{ category.href }}">Перейти к каталогу</a>#}
  </div>
  {% endfor %}
</div>

<div class="button-order">
  <a href="#orderModal1" rel="modal:open">Заказать монтаж из моего оборудования </a>
  <a href="#orderModal2" rel="modal:open">Подобрать оборудование под мою задачу</a>
</div>
